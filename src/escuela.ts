//esto es una interfaz de la entidad escuela
export interface Escuela {
    id: number;         // Identificador único de la escuela
    nombre: string;     // nombre de la escuela
    creditos: number;     // Cantidad de creditos de la escuela
    numEstudiantes: number;   // Numero de estudiantes de la escuela
}